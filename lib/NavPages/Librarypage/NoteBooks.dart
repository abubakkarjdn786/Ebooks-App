import 'package:flutter/material.dart';
class NoteBooks extends StatelessWidget {
  const NoteBooks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Container(
        child:Center(child: Text('Notebooks')),
      ),
    );
  }
} 
